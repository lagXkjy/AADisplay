<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/cv_handle"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:background="@drawable/bg_window_controller"
    android:elevation="4dp">

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="4dp">

        <ImageButton
            android:id="@+id/ib_handle"
            android:layout_width="@dimen/controller_icon_size"
            android:layout_height="@dimen/controller_icon_size"
            android:background="?android:attr/selectableItemBackground"
            android:padding="4dp"
            android:src="@drawable/ic_monitor_24"
            android:contentDescription="AADisplay" />

        <Button
            android:id="@+id/tv_destroy_time"
            android:layout_width="@dimen/controller_icon_size"
            android:layout_height="@dimen/controller_icon_size"
            android:layout_marginTop="4dp"
            android:singleLine="true"
            android:padding="0dp"
            android:textSize="@dimen/controller_text_size"
            android:background="?android:attr/selectableItemBackground"
            android:textColor="@color/color_on_primary_container"
            android:visibility="gone"
            tools:text="180s"
            tools:visibility="visible" />

    </LinearLayout>
</FrameLayout>
