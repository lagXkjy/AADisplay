# AADisplay 0.24#17.4-r9 版本更新

相对上一版 r8，这次重点是「每窗多应用保活」和一批分屏 / 全屏日常会踩到的问题修复。

## 新功能 / 体验优化

### 每窗最多 3 个应用，切顶不用重开

- 左、右虚拟屏各自可保活最多 **3** 个应用：选新应用时旧应用进栈，不再直接杀掉。
- 栈顶才是当前画面；在 Recent 里点选或拖到顶部，只把已有任务拉到前面，页面状态尽量保持。
- 再加第 4 个时会挤出栈底最旧的那个。
- Recent 左 / 中列底部有「添加应用」，可在不挤掉当前显示的前提下往栈里加。
- 断线重连、冷启动仍会按上次的栈序与栈顶恢复（兼容旧的单包记忆）。

### Recent 更好点

- 左 / 中列三项均分高度，三应用同屏可见，一般不用上下滚。
- 点图标、标题或灰底空白都会置顶并关掉面板；Close 仍只负责关掉该任务。

## 问题修复

### 多应用栈切对不对

- 修了「列表以为置顶了，画面还是旧应用」：含全屏背后那一侧、高德 / Google 地图等同屏场景。
- 同栈点选后不再被底层应用抢回前台。
- 分隔条交换左右后，两侧窗口会铺满各自虚拟屏（减少黑边 / 旧尺寸残留）。

### 全屏 peel

- 长按只震动提示，**松手后再打开** Recent，避免随后 peel / 触控「点不动」。
- 从全屏拖回分屏后，两侧分辨率 / 窗口宽度更不容易卡在中间态。

### 其它

- 连接车机使用期间，减少「车机在用、手机却被亮屏」的情况（仍优先保证三星车机虚拟屏不黑）。

## 使用提示

1. 安装后请在 LSPosed 确认已启用 **System Framework** + **Android Auto**，并重启一次。
2. 无桌面图标：从 LSPosed → AADisplay 打开激活状态页。
3. 详细技术变更与真机核对项见 [CHANGELOG.md](CHANGELOG.md)。
